# Administration

The administration area is available only to `admin` users. It covers application configuration, rendering, plugins, users, groups, page inventory, navigation icons, reusable content, tags, tokens, imports/exports, media cleanup, documentation health, audit history, and the recycle bin.

{{subpages}}
